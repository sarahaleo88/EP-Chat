# NPM Configuration for EP Chat
# Ensure npm uses only the local package-lock.json

# Use local lockfile only
package-lock=true

# Security settings
audit-level=moderate
fund=false

# Performance settings
prefer-offline=false

# Registry settings (use default npm registry)
registry=https://registry.npmjs.org/
